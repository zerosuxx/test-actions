name: Build

on: 
  push:
  workflow_dispatch:

env:
  IMAGE_TAG: '2.7.99-enhanced.1'

jobs:
  build:
    runs-on: 'ubuntu-latest'
    steps:
      - uses: actions/checkout@v5
        with:
          repository: 'zerosuxx/sftpgo'
          ref: ${{ env.IMAGE_TAG }}
